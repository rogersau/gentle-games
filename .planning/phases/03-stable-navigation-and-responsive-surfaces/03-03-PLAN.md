---
phase: 03-stable-navigation-and-responsive-surfaces
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BubbleField.tsx
  - src/components/BubbleField.test.tsx
  - src/screens/BubbleScreen.test.tsx
  - src/components/GlitterGlobe.tsx
  - src/components/GlitterGlobe.test.tsx
  - src/screens/GlitterScreen.test.tsx
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "Bubble Pop and Glitter Fall stay responsive during ongoing motion because their hottest loops stop publishing multiple React state updates per frame."
    - "Bubble popping, wake trails, shake behavior, and screen-level controls still work after the churn reduction."
    - "The audited motion surfaces remain calm and visually familiar while the internals become cheaper to update."
  artifacts:
    - path: "src/components/BubbleField.tsx"
      provides: "Single-snapshot Bubble Pop frame publication backed by refs"
      contains: "requestAnimationFrame"
    - path: "src/components/BubbleField.test.tsx"
      provides: "Direct BubbleField cleanup and interaction regression coverage"
      contains: "requestAnimationFrame"
    - path: "src/components/GlitterGlobe.tsx"
      provides: "Single-snapshot Glitter motion publication for particles and wake ripples"
      contains: "requestAnimationFrame"
    - path: "src/components/GlitterGlobe.test.tsx"
      provides: "Regression coverage for motion cleanup, imperative actions, and shake/wake behavior"
      contains: "cleans up animation frame on unmount"
  key_links:
    - from: "src/components/BubbleField.tsx"
      to: "bubble and pop-indicator rendering"
      via: "one published frame snapshot"
      pattern: "requestAnimationFrame"
    - from: "src/components/GlitterGlobe.tsx"
      to: "particle and wake rendering"
      via: "single published frame state"
      pattern: "particles|wakeRipples"
    - from: "src/screens/BubbleScreen.test.tsx"
      to: "src/components/BubbleField.tsx"
      via: "screen-level pop callback contract"
      pattern: "onBubblePop"
---

<objective>
Reduce the hottest per-frame React churn on the audited motion-heavy surfaces while keeping the current calm visuals and interaction behavior.

Purpose: Improve responsiveness on lower-end devices by publishing one visible frame snapshot per loop instead of repeatedly mutating separate React state atoms.
Output: Localized motion-loop refactors for Bubble Field and Glitter Globe plus focused component/screen regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stable-navigation-and-responsive-surfaces/03-CONTEXT.md
@.planning/phases/03-stable-navigation-and-responsive-surfaces/03-RESEARCH.md
@src/components/BubbleField.tsx
@src/screens/BubbleScreen.test.tsx
@src/components/GlitterGlobe.tsx
@src/components/GlitterGlobe.test.tsx
@src/screens/GlitterScreen.test.tsx

<interfaces>
Current Bubble Field seam:
```typescript
interface BubbleFieldProps {
  width: number;
  height: number;
  minActiveBubbles?: number;
  maxActiveBubbles?: number;
  spawnIntervalMs?: number;
  onBubblePop?: () => void;
}

const [bubbles, setBubbles] = useState<Bubble[]>(...);
const [popIndicators, setPopIndicators] = useState<PopIndicator[]>([]);
```

Current Glitter Globe seam:
```typescript
export interface GlitterGlobeRef {
  addGlitter: (count?: number) => void;
  clearGlitter: () => void;
}

const [particles, setParticles] = useState<GlitterParticle[]>(...);
const [wakeRipples, setWakeRipples] = useState<WakeRipple[]>([]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse Bubble Field motion updates into one published frame snapshot</name>
  <files>src/components/BubbleField.tsx, src/components/BubbleField.test.tsx, src/screens/BubbleScreen.test.tsx</files>
  <behavior>
    - Test 1: BubbleField cancels its animation frame on unmount and keeps minimum/spawn behavior intact.
    - Test 2: A pop interaction still removes the tapped bubble, creates the indicator, and calls `onBubblePop`.
    - Test 3: BubbleScreen still increments its popped counter and plays the pop sound through the existing callback contract.
  </behavior>
  <action>Refactor `BubbleField.tsx` so the RAF loop mutates ref-backed bubble/pop-indicator data and publishes one render snapshot per frame instead of calling separate `setBubbles` and `setPopIndicators` updates. Keep current spawn timing, minimum bubble guarantees, tap hit-testing, accessibility label, and calm visuals unchanged. Add a new direct `src/components/BubbleField.test.tsx` file that covers frame cleanup and pop behavior, and adjust `BubbleScreen.test.tsx` only as needed to preserve the existing screen-level contract. Do not add animation libraries or redesign the field.</action>
  <verify>
    <automated>npm test -- --runInBand src/components/BubbleField.test.tsx src/screens/BubbleScreen.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Bubble Field publishes a single visible snapshot per RAF tick, preserves pop behavior, and has direct churn-sensitive regression coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse Glitter Globe particle and wake updates into one published frame snapshot</name>
  <files>src/components/GlitterGlobe.tsx, src/components/GlitterGlobe.test.tsx, src/screens/GlitterScreen.test.tsx</files>
  <behavior>
    - Test 1: GlitterGlobe still cleans up RAF and accelerometer listeners on unmount.
    - Test 2: Imperative add/clear controls and wake-ripple interactions still work after the state publication refactor.
    - Test 3: GlitterScreen still wires its clear and sprinkle controls to the globe ref.
  </behavior>
  <action>Refactor `GlitterGlobe.tsx` so particles and wake ripples live in ref-backed mutable data and the component publishes a single render snapshot for each visible update. Keep the existing accelerometer shake behavior, touch wake trail behavior, imperative `addGlitter`/`clearGlitter` API, and current visuals intact. Update `GlitterGlobe.test.tsx` to cover cleanup plus the refactored wake/particle behavior, and keep `GlitterScreen.test.tsx` verifying the same screen controls. Do not replace the current SVG/Accelerometer stack or widen this into a full animation-system rewrite.</action>
  <verify>
    <automated>npm test -- --runInBand src/components/GlitterGlobe.test.tsx src/screens/GlitterScreen.test.tsx src/components/BubbleField.test.tsx src/screens/BubbleScreen.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Glitter Globe stops publishing multiple React state updates through its hottest paths while preserving the current motion, wake, shake, and screen-control behavior.</done>
</task>

</tasks>

<verification>
Run `npm test -- --runInBand src/components/BubbleField.test.tsx src/screens/BubbleScreen.test.tsx src/components/GlitterGlobe.test.tsx src/screens/GlitterScreen.test.tsx` and `npm run typecheck`.
</verification>

<success_criteria>
- Bubble Field and Glitter Globe publish one visible frame snapshot per update loop instead of multiple state writes.
- Existing pop, wake, shake, and screen-control behavior remains intact.
- The new motion-surface regressions pass without adding new libraries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stable-navigation-and-responsive-surfaces/03-03-SUMMARY.md`
</output>
