---
phase: 03-stable-navigation-and-responsive-surfaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/KeepyUppyBoard.tsx
  - src/components/KeepyUppyBoard.test.tsx
  - src/screens/BreathingGardenScreen.tsx
  - src/screens/BreathingGardenScreen.test.tsx
autonomous: true
requirements:
  - STAB-04
must_haves:
  truths:
    - "Keepy Uppy score, balloon-count, and popped-count callbacks stay consistent during repeated interactions without zero-delay timer hacks."
    - "Breathing Garden phase and count labels stay stable across repeated renders without hidden dependency suppression."
    - "The audited surfaces behave the same to the player while their state flow becomes explicit and easier to change safely."
  artifacts:
    - path: "src/components/KeepyUppyBoard.tsx"
      provides: "Post-commit callback publication without `setTimeout(..., 0)`"
      contains: "useEffect"
    - path: "src/components/KeepyUppyBoard.test.tsx"
      provides: "Regression coverage for callback publication and reset behavior without timer flushing"
      contains: "reset"
    - path: "src/screens/BreathingGardenScreen.tsx"
      provides: "Explicit animation effect ownership with stable refs"
      contains: "Animated.Value"
    - path: "src/screens/BreathingGardenScreen.test.tsx"
      provides: "Regression coverage for repeated renders and animation-disabled behavior"
      contains: "renders without infinite loop errors"
  key_links:
    - from: "src/components/KeepyUppyBoard.tsx"
      to: "onScoreChange/onBalloonCountChange/onPoppedChange"
      via: "post-commit effects and stable refs"
      pattern: "onScoreChangeRef|useEffect"
    - from: "src/screens/BreathingGardenScreen.tsx"
      to: "BreathingBall callbacks"
      via: "dependency-complete effects reacting to phase/progress changes"
      pattern: "onPhaseChange|onProgress"
    - from: "src/screens/BreathingGardenScreen.tsx"
      to: "Animated label opacity refs"
      via: "stable ref ownership instead of suppressed deps"
      pattern: "useRef\\(new Animated.Value"
---

<objective>
Remove the audited timing and dependency hacks from Keepy Uppy and Breathing Garden without changing visible gameplay or calm breathing behavior.

Purpose: Make the riskiest hook-driven surfaces predictable under repeated renders, resets, and callbacks so later changes do not depend on hidden timing quirks.
Output: Explicit effect-owned state publication on Keepy Uppy, stable animation effects on Breathing Garden, and focused regressions for both seams.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stable-navigation-and-responsive-surfaces/03-CONTEXT.md
@.planning/phases/03-stable-navigation-and-responsive-surfaces/03-RESEARCH.md
@src/components/KeepyUppyBoard.tsx
@src/components/KeepyUppyBoard.test.tsx
@src/screens/BreathingGardenScreen.tsx
@src/screens/BreathingGardenScreen.test.tsx

<interfaces>
Current Keepy Uppy contract:
```typescript
export interface KeepyUppyBoardRef {
  addBalloon: () => void;
  resetBalloons: () => void;
  getBalloonCount: () => number;
}

interface KeepyUppyBoardProps {
  bounds: KeepyUppyBounds;
  onScoreChange?: (score: number) => void;
  onBalloonCountChange?: (count: number) => void;
  onPoppedChange?: (popped: number) => void;
  easyMode?: boolean;
}
```

Current fragile seams:
```typescript
// src/components/KeepyUppyBoard.tsx
setTimeout(() => {
  onBalloonCountChangeRef.current?.(balloons.length);
}, 0);
// eslint-disable-next-line react-hooks/exhaustive-deps
```

```typescript
// src/screens/BreathingGardenScreen.tsx
const countOpacity = useRef(new Animated.Value(0)).current;
const phaseOpacity = useRef(new Animated.Value(1)).current;
// eslint-disable-next-line react-hooks/exhaustive-deps
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace Keepy Uppy deferred callback hacks with post-commit publication</name>
  <files>src/components/KeepyUppyBoard.tsx, src/components/KeepyUppyBoard.test.tsx</files>
  <behavior>
    - Test 1: Initial balloon-count, score, and reset callbacks publish from committed state without advancing zero-delay timers.
    - Test 2: Repeated add/reset interactions keep callback payloads aligned with the rendered balloon count.
    - Test 3: The board still preserves its imperative ref API and existing balloon cap/easy-mode gameplay behavior.
  </behavior>
  <action>Refactor `KeepyUppyBoard.tsx` so callback notification happens from explicit post-commit effects and stable refs instead of `setTimeout(..., 0)` deferrals and suppressed dependency arrays. Keep the existing `KeepyUppyBoardRef` API, balloon stepping cadence, balloon cap, and tap/flick interactions unchanged. Let reset paths update local state synchronously, then publish the committed zero values from effects rather than deferred timer callbacks. Expand `KeepyUppyBoard.test.tsx` so the new regressions assert callback behavior without relying on timer flushing just to observe state publication. Do not widen this into a logic rewrite of `keepyUppyLogic.ts`.</action>
  <verify>
    <automated>npm test -- --runInBand src/components/KeepyUppyBoard.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Keepy Uppy no longer relies on zero-delay timers or hook-dependency suppression to keep parent callbacks in sync with board state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Breathing Garden animation effects dependency-complete and stable</name>
  <files>src/screens/BreathingGardenScreen.tsx, src/screens/BreathingGardenScreen.test.tsx</files>
  <behavior>
    - Test 1: Repeated renders do not trigger infinite-loop warnings after the dependency suppressions are removed.
    - Test 2: Phase label transitions still show the correct inhale/exhale text with animations enabled and disabled.
    - Test 3: Color cycling, music toggling, and back navigation still work after the effect cleanup.
  </behavior>
  <action>Refactor `BreathingGardenScreen.tsx` so `Animated.Value` ownership stays in stable refs and every effect lists the state it truly depends on. Keep the user-visible breathing cadence, color-change button, music controls, and calm copy unchanged. Replace the current suppressed-dependency animation sequencing with explicit helpers/effects that react to scalar state (`phase`, `displayedPhase`, `progress`, `settings.animationsEnabled`) and avoid hidden mutable timing assumptions. Expand `BreathingGardenScreen.test.tsx` to lock in repeated-render safety plus animation-enabled/disabled behavior. Do not redesign the screen or replace the existing animation stack.</action>
  <verify>
    <automated>npm test -- --runInBand src/screens/BreathingGardenScreen.test.tsx src/components/KeepyUppyBoard.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Breathing Garden keeps its current calm behavior while the audited effects run without dependency suppression or hidden timing hacks.</done>
</task>

</tasks>

<verification>
Run `npm test -- --runInBand src/components/KeepyUppyBoard.test.tsx src/screens/BreathingGardenScreen.test.tsx` and `npm run typecheck`.
</verification>

<success_criteria>
- Keepy Uppy state publication is explicit and no longer depends on `setTimeout(..., 0)` for correctness.
- Breathing Garden no longer suppresses hook dependencies to stay stable.
- The player-facing behavior on both audited surfaces remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stable-navigation-and-responsive-surfaces/03-02-SUMMARY.md`
</output>
