---
phase: 03-error-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App.tsx
  - src/screens/HomeScreen.tsx
  - src/i18n/locales/en-AU.json
  - src/i18n/locales/en-US.json
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
must_haves:
  truths:
    - "LetterLanternScreen.tsx no longer exists in codebase"
    - "StarPathScreen.tsx no longer exists in codebase"
    - "App.tsx does not import or reference LetterLantern or StarPath"
    - "HomeScreen.tsx ROUTE_MAP does not contain letter-lantern or star-path routes"
    - "Translation files no longer contain letterLanterns or starPath keys"
    - "App builds and runs without errors after cleanup"
  artifacts:
    - path: "App.tsx"
      provides: "Clean navigation without removed game screens"
      lacks: ["LetterLanternScreen import", "StarPathScreen import", "LetterLantern route", "StarPath route"]
    - path: "src/screens/HomeScreen.tsx"
      provides: "Game list without removed games"
      lacks: ["letter-lantern in ROUTE_MAP", "star-path in ROUTE_MAP"]
    - path: "src/i18n/locales/en-AU.json"
      provides: "Translations without removed game strings"
      lacks: ["letterLanterns section", "starPath section", "unfinishedGames description referencing Letter Lanterns or Star Path"]
    - path: "src/i18n/locales/en-US.json"
      provides: "Translations without removed game strings"
      lacks: ["letterLanterns section", "starPath section", "unfinishedGames description referencing Letter Lanterns or Star Path"]
  key_links:
    - from: "App.tsx"
      to: "Stack.Navigator"
      pattern: "Stack.Screen components for each game route"
    - from: "HomeScreen.tsx"
      to: "ROUTE_MAP"
      pattern: "Route mapping from game ID to navigation name"
---

<objective>
Completely remove LetterLantern and StarPath games from the codebase. These games were deleted post-v1.0 and all references must be cleaned up including screens, navigation routes, translations, and settings references.

Purpose: Ensure clean codebase with no dead code or broken references before adding error monitoring.
Output: App builds and runs successfully without LetterLantern or StarPath.
</objective>

<execution_context>
@C:/Users/james/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-error-monitoring/03-CONTEXT.md
@App.tsx
@src/screens/HomeScreen.tsx
@src/i18n/locales/en-AU.json
@src/i18n/locales/en-US.json

## Current State

### App.tsx (lines 23-24, 60-61)
Currently imports and registers routes for removed games:
```typescript
import { LetterLanternScreen } from './src/screens/LetterLanternScreen';
import { StarPathScreen } from './src/screens/StarPathScreen';
// ...
<Stack.Screen name="LetterLantern" component={LetterLanternScreen} />
<Stack.Screen name="StarPath" component={StarPathScreen} />
```

### HomeScreen.tsx (lines 93-103)
ROUTE_MAP contains entries for removed games:
```typescript
const ROUTE_MAP: Record<string, string> = {
  // ... other games
  "letter-lantern": "LetterLantern",
  "star-path": "StarPath",
};
```

Note: The GAMES array (lines 27-91) does NOT include letter-lantern or star-path - they were already excluded from the visible game list.

### Translation Files (en-AU.json and en-US.json)
Contain full translation sections for removed games:
- `games.letterLanterns` (lines 241-254 in en-AU.json)
- `games.starPath` (lines 255-266 in en-AU.json)
- `settings.unfinishedGames.description` references these games (line 313)

### Screen Files
LetterLanternScreen.tsx and StarPathScreen.tsx have already been deleted (files don't exist per glob search).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove LetterLantern and StarPath from App.tsx navigation</name>
  <files>App.tsx</files>
  <action>
Remove all references to LetterLantern and StarPath from App.tsx:

1. Delete imports (lines 23-24):
   - Remove: `import { LetterLanternScreen } from './src/screens/LetterLanternScreen';`
   - Remove: `import { StarPathScreen } from './src/screens/StarPathScreen';`

2. Delete Stack.Screen registrations (lines 60-61):
   - Remove: `<Stack.Screen name="LetterLantern" component={LetterLanternScreen} />`
   - Remove: `<Stack.Screen name="StarPath" component={StarPathScreen} />`

Preserve all other imports and routes. Maintain alphabetical order if the routes were alphabetical (they appear to be: BreathingGarden, Bubble, CategoryMatch, Drawing, Game, Glitter, Home, KeepyUppy, NumberPicnic, PatternTrain remain).

TypeScript will likely show errors for the missing imports initially - this is expected since the screen files were already deleted.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>App.tsx compiles without LetterLanternScreen or StarPathScreen imports/routes, all other routes still functional</done>
</task>

<task type="auto">
  <name>Task 2: Remove LetterLantern and StarPath from HomeScreen route mapping</name>
  <files>src/screens/HomeScreen.tsx</files>
  <action>
Remove route mappings for the deleted games from HomeScreen.tsx:

1. Edit ROUTE_MAP (lines 93-103) to remove entries:
   - Remove: `"letter-lantern": "LetterLantern",`
   - Remove: `"star-path": "StarPath",`

The ROUTE_MAP should only contain mappings for games that still exist:
- memory-snap → Game
- drawing → Drawing
- glitter-fall → Glitter
- bubble-pop → Bubble
- category-match → CategoryMatch
- keepy-uppy → KeepyUppy
- breathing-garden → BreathingGarden
- pattern-train → PatternTrain
- number-picnic → NumberPicnic
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>HomeScreen.tsx ROUTE_MAP contains only valid game routes, no references to letter-lantern or star-path</done>
</task>

<task type="auto">
  <name>Task 3: Remove LetterLantern and StarPath translations</name>
  <files>src/i18n/locales/en-AU.json, src/i18n/locales/en-US.json</files>
  <action>
Remove all translation keys related to LetterLantern and StarPath from both translation files:

For en-AU.json and en-US.json:

1. Remove the entire `games.letterLanterns` object (lines 241-254 in en-AU.json, 239-252 in en-US.json)
   - Includes: name, description, title, nextLetter, subtitle, targetLabel, lanternsLit, feedback

2. Remove the entire `games.starPath` object (lines 255-266 in en-AU.json, 253-264 in en-US.json)
   - Includes: name, description, title, resetPath, subtitle, collected, tilt, tiltOff, tiltOn, complete

3. Update `settings.unfinishedGames.description` (line 313 in en-AU.json, 311 in en-US.json):
   - Current: "Show games that are still in development (Number Picnic, Letter Lanterns, Star Path)"
   - Change to: "Show games that are still in development (Number Picnic)"
   - Since NumberPicnic is now the only unfinished game

Make identical changes to both en-AU.json and en-US.json files. Preserve JSON structure and formatting (trailing commas if present).
  </action>
  <verify>
    <automated>npm run test:i18n</automated>
  </verify>
  <done>Both translation files no longer contain letterLanterns or starPath keys, settings.unfinishedGames.description updated to only mention Number Picnic</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` passes with no errors
- [ ] `npm run test` passes (tests should not reference removed games)
- [ ] App can be started (`npm start` or `expo start`) without import errors
- [ ] HomeScreen displays only valid games (no missing references)
</verification>

<success_criteria>
1. LetterLanternScreen and StarPathScreen completely removed from all imports and route registrations
2. Translation files cleaned of all references to letterLanterns and starPath
3. App builds and runs successfully
4. All existing tests pass
5. No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-monitoring/03-01-SUMMARY.md`
</output>
