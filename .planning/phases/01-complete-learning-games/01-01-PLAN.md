---
phase: 01-complete-learning-games
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/screens/NumberPicnicScreen.tsx
  - src/screens/LetterLanternScreen.tsx
  - src/utils/numberPicnicLogic.ts
  - src/utils/letterLanternLogic.ts
  - src/i18n/locales/en-AU.json
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-03
  - LEARN-04
  - LEARN-05
---

<objective>
Make the three hidden learning games (NumberPicnic, LetterLantern, StarPath) fully available and visually complete.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-complete-learning-games/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable games by default</name>
  <files>src/types/index.ts</files>
  <action>
    Remove or disable the enableUnfinishedGames setting so all games appear on home screen by default.

    Option A: Remove 'number-picnic', 'letter-lanterns', 'star-path' from UNFINISHED_GAMES array in src/types/index.ts
    Option B: In SettingsContext defaultSettings, set enableUnfinishedGames: true

    Choose the simpler approach - likely Option B since it keeps the setting but defaults to enabled.
  </action>
  <verify>
    <automated>grep -q "enableUnfinishedGames.*true" src/context/SettingsContext.tsx</automated>
  </verify>
  <done>All 11 games visible on home screen including NumberPicnic, LetterLantern, StarPath</done>
</task>

<task type="auto">
  <name>Task 2: Add dot representations to NumberPicnic</name>
  <files>src/screens/NumberPicnicScreen.tsx, src/utils/numberPicnicLogic.ts, src/i18n/locales/en-AU.json</files>
  <action>
    Add visual dot representations to NumberPicnic that show dots matching the target number.

    1. In numberPicnicLogic.ts: Update generateNumberPicnicPrompt to include a visualDots array with that many emoji dots (🟢)
    2. In NumberPicnicScreen.tsx: Display the visualDots in the prompt card - show dots in a row below the text prompt
    3. Add any needed translation keys for the dots display

    The dots should display in the previewItems area where items are currently shown.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>NumberPicnic shows visual dots representing the target number (e.g., 3 shows 🟢🟢🟢)</done>
</task>

<task type="auto">
  <name>Task 3: Add item images to LetterLantern</name>
  <files>src/screens/LetterLanternScreen.tsx, src/utils/letterLanternLogic.ts, src/i18n/locales/en-AU.json</files>
  <action>
    Add visual item representations to LetterLantern that show items starting with the target letter.

    1. In letterLanternLogic.ts: Update generateLetterLanternRound to include itemEmojis array with 3-4 items that start with the target letter
    2. In LetterLanternScreen.tsx: Display these items in or below the target letter card
    3. Add any needed translation keys

    Example: Letter "A" shows 🍎🐱✈️ as hint items
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | head -20</automated>
  </verify>
  <done>LetterLantern shows item emojis that start with the target letter</done>
</task>

</tasks>

<verification>
- [ ] All games visible on home screen
- [ ] NumberPicnic displays dot representations
- [ ] LetterLantern displays item emojis
- [ ] StarPath already works (already implemented)
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
Phase 1 complete when:
- LEARN-01: NumberPicnic playable (already works, just needs enabling)
- LEARN-02: NumberPicnic has visual representations (dots added)
- LEARN-03: LetterLantern playable (already works, just needs enabling)
- LEARN-04: LetterLantern has visual representations (items added)
- LEARN-05: StarPath playable (already works)
</success_criteria>

<output>
After completion, create `.planning/phases/01-complete-learning-games/01-01-SUMMARY.md`
</output>
