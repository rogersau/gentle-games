---
phase: 02-polish-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/context/SettingsContext.tsx
  - src/screens/SettingsScreen.tsx
  - src/utils/theme.ts
autonomous: true
requirements:
  - ACCESS-03
user_setup: []

must_haves:
  truths:
    - "Reduced motion setting exists in Settings"
    - "System prefers-reduced-motion is detected"
    - "Reduced motion provides lighter animations (not disabled)"
    - "Setting visible in Settings UI alongside theme options"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Settings type with reducedMotionEnabled field"
    - path: "src/context/SettingsContext.tsx"
      provides: "Settings state and update with reducedMotionEnabled"
    - path: "src/screens/SettingsScreen.tsx"
      provides: "UI toggle for reduced motion setting"
  key_links:
    - from: "SettingsContext"
      to: "SettingsScreen"
      via: "useSettings hook reads settings.reducedMotionEnabled"
    - from: "theme.ts"
      to: "system preference"
      via: "React Native accessibility API for prefersReducedMotion"
---

<objective>
Add reduced motion support as a separate setting. Users should be able to enable reduced motion independently from animations, and the system preference should be detected automatically.
</objective>

<execution_context>
@C:/Users/james/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/02-polish-accessibility/02-CONTEXT.md

# Key Decisions from Context:
- Add separate setting - new reduced motion toggle, not linked to animationsEnabled
- Separate options in Settings UI - show alongside light/dark/system theme options
- Use React Native API - detect system prefers-reduced-motion
- Lighter animation - reduced motion = fewer/simpler animations (not instant/off)
- Show in Settings UI - visible toggle for users
- Reduced motion affects animations but differently than disabling them entirely
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reducedMotionEnabled to Settings type</name>
  <files>
    - src/types/index.ts
  </files>
  <action>
    Find the Settings interface/type in src/types/index.ts.
    Add reducedMotionEnabled: boolean field to the Settings type.
    Default should be false (or system preference if detectable at type definition).
  </action>
  <verify>
    <automated>grep -n "reducedMotionEnabled" src/types/index.ts</automated>
  </verify>
  <done>Settings type includes reducedMotionEnabled</done>
</task>

<task type="auto">
  <name>Task 2: Add reduced motion to SettingsContext</name>
  <files>
    - src/context/SettingsContext.tsx
  </files>
  <action>
    1. Add reducedMotionEnabled to defaultSettings with default value false
    2. Add to sanitizeSettings function to parse the value
    3. Export useSettings returns settings.reducedMotionEnabled
    
    Note: This is a user-toggle setting, not auto-detected system preference.
    The system preference detection will be handled in theme.ts.
  </action>
  <verify>
    <automated>grep -n "reducedMotion" src/context/SettingsContext.tsx</automated>
  </verify>
  <done>SettingsContext manages reducedMotionEnabled state</done>
</task>

<task type="auto">
  <name>Task 3: Add reduced motion toggle to SettingsScreen</name>
  <files>
    - src/screens/SettingsScreen.tsx
  </files>
  <action>
    Find where theme/colorMode toggle is in SettingsScreen.
    Add a new toggle switch for "Reduced Motion" near the theme options.
    Label: "Reduced Motion" or similar friendly text.
    Use i18n translation key.
    Default state: off (false).
  </action>
  <verify>
    <automated>grep -n "reducedMotion\|Reduced Motion" src/screens/SettingsScreen.tsx</automated>
  </verify>
  <done>Reduced motion toggle visible in Settings UI</done>
</task>

<task type="auto">
  <name>Task 4: Implement system reduced motion detection</name>
  <files>
    - src/utils/theme.ts
  </files>
  <action>
    Add system reduced motion detection using React Native's AccessibilityInfo.
    Import AccessibilityInfo from react-native.
    
    Create a hook or function that:
    1. Uses AccessibilityInfo.get() or listens to reduceMotionChanged
    2. Returns whether user prefers reduced motion at system level
    
    This complements the user toggle - if user hasn't set preference,
    we can use system preference. But per context, we have a visible toggle.
    
    Export the detection for use in animation components.
  </action>
  <verify>
    <automated>grep -n "AccessibilityInfo\|reducedMotion" src/utils/theme.ts</automated>
  </verify>
  <done>System reduced motion preference is detectable</done>
</task>

<task type="auto">
  <name>Task 5: Wire reduced motion to animation handling</name>
  <files>
    - src/utils/theme.ts
    - src/screens/*Screen.tsx (update to use)
  </files>
  <action>
    Create a useShouldReduceMotion hook that combines:
    - settings.reducedMotionEnabled (user toggle)
    - System preference if available
    
    Export this hook from theme.ts for use in game screens.
    
    Update game screens to check shouldReduceMotion:
    - If true: use lighter/faster animations (not instant, just reduced)
    - If false: normal animations
    
    This is different from animationsEnabled=false which disables entirely.
  </action>
  <verify>
    <automated>grep -rn "useShouldReduceMotion\|shouldReduceMotion" src/ | head -10</automated>
  </verify>
  <done>Reduced motion setting affects animation behavior in games</done>
</task>

</tasks>

<verification>
- Reduced motion toggle appears in Settings
- Toggle is functional (saves to settings)
- System reduced motion can be detected
- When enabled, animations are lighter (not disabled)
</verification>

<success_criteria>
ACCESS-03: Reduced motion support implemented with separate setting and system detection
</success_criteria>

<output>
After completion, create `.planning/phases/02-polish-accessibility/02-02-SUMMARY.md`
</output>
