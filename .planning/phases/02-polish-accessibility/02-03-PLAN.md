---
phase: 02-polish-accessibility
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/screens/HomeScreen.tsx
  - src/screens/SettingsScreen.tsx
  - src/screens/NumberPicnicScreen.tsx
  - src/screens/LetterLanternScreen.tsx
  - src/screens/StarPathScreen.tsx
  - src/screens/BubbleScreen.tsx
  - src/screens/KeepyUppyScreen.tsx
  - src/screens/CategoryMatchScreen.tsx
  - src/screens/PatternTrainScreen.tsx
  - src/screens/GlitterScreen.tsx
  - src/screens/DrawingScreen.tsx
  - src/screens/BreathingGardenScreen.tsx
  - src/ui/components/*.tsx
autonomous: true
requirements:
  - ACCESS-01
  - POLISH-03
user_setup: []

must_haves:
  truths:
    - "All touch targets meet 48dp minimum"
    - "All screens audited for touch target sizes"
    - "All issues fixed by adjusting spacing/padding"
    - "New games follow existing UI patterns"
  artifacts:
    - path: "src/screens/*Screen.tsx"
      provides: "All screens with touch targets meeting 48dp"
    - path: "src/ui/components/"
      provides: "Components with consistent patterns"
  key_links:
    - from: "Touch target audit"
      to: "Each screen"
      via: "StyleSheet padding/margin inspection"
---

<objective>
Audit and fix touch targets across all screens to meet 48dp minimum, and ensure new learning games follow consistent UI patterns.
</objective>

<execution_context>
@C:/Users/james/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/02-polish-accessibility/02-CONTEXT.md

# Key Decisions from Context:
- Scope: All screens (every interactive element)
- Minimum size: 48dp (Android accessibility guidelines)
- Fix by adjusting spacing: increase padding/margins
- Audit everything: buttons, game elements, navigation, inputs
- Fix all issues: don't just document, actually fix
- UI consistency: full design system (fonts, spacing, colors)
- New games first: NumberPicnic, LetterLantern, StarPath

# Note:
Light/dark mode is already implemented - no work needed (ACCESS-02 complete)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit touch targets in all screens</name>
  <files>
    - src/screens/HomeScreen.tsx
    - src/screens/SettingsScreen.tsx
    - src/screens/NumberPicnicScreen.tsx
    - src/screens/LetterLanternScreen.tsx
    - src/screens/StarPathScreen.tsx
    - src/screens/BubbleScreen.tsx
    - src/screens/KeepyUppyScreen.tsx
    - src/screens/CategoryMatchScreen.tsx
    - src/screens/PatternTrainScreen.tsx
    - src/screens/GlitterScreen.tsx
    - src/screens/DrawingScreen.tsx
    - src/screens/BreathingGardenScreen.tsx
  </files>
  <action>
    For each screen, search for TouchableWithoutFeedback, TouchableOpacity, Pressable, 
    Button components, and any onPress handlers.
    
    Note the dimensions/padding of each interactive element.
    
    Common patterns to check:
    - Game choice buttons
    - Navigation elements
    - Settings toggles
    - Game answer/selection buttons
    
    Document which elements are below 48dp.
  </action>
  <verify>
    <automated>grep -rn "TouchableOpacity\|Pressable\|onPress" src/screens/*.tsx | wc -l</interactive>
  </verify>
  <done>List of screens with touch target issues documented</done>
</task>

<task type="auto">
  <name>Task 2: Fix touch target sizes</name>
  <files>
    - src/screens/NumberPicnicScreen.tsx
    - src/screens/LetterLanternScreen.tsx
    - src/screens/StarPathScreen.tsx
    - src/screens/HomeScreen.tsx
    - src/screens/SettingsScreen.tsx
  </files>
  <action>
    For each screen with touch targets below 48dp:
    1. Identify the StyleSheet style with the issue
    2. Increase padding or minHeight to meet 48dp minimum
    3. Common fix: padding: 12 (24dp total with default) or minHeight: 48
    
    Focus on:
    - Game choice buttons
    - Home screen game cards
    - Settings toggles
    
    Use consistent values across screens.
  </action>
  <verify>
    <automated>Manual: Run app, verify all touch targets are at least 48dp</automated>
  </verify>
  <done>All identified touch target issues fixed</done>
</task>

<task type="auto">
  <name>Task 3: Verify new games UI consistency</name>
  <files>
    - src/screens/NumberPicnicScreen.tsx
    - src/screens/LetterLanternScreen.tsx
    - src/screens/StarPathScreen.tsx
  </files>
  <action>
    Compare the new games (NumberPicnic, LetterLantern, StarPath) to existing games
    (Bubble, KeepyUppy, CategoryMatch) for:
    
    1. Button styles - same borderRadius, shadows, fonts?
    2. Layout patterns - similar spacing, alignment?
    3. Feedback states - same highlight/press states?
    4. Color usage - consistent with palette?
    
    Document any inconsistencies found.
  </action>
  <verify>
    <automated>Manual: Visual comparison of new vs existing games</automated>
  </verify>
  <done>List of consistency issues documented</done>
</task>

<task type="auto">
  <name>Task 4: Fix UI consistency issues</name>
  <files>
    - src/screens/NumberPicnicScreen.tsx
    - src/screens/LetterLanternScreen.tsx
    - src/screens/StarPathScreen.tsx
  </files>
  <action>
    Apply fixes to make new games consistent with existing games:
    
    1. Button styles - match borderRadius, shadows from existing games
    2. Spacing - use consistent padding/margins
    3. Colors - use PASTEL_COLORS consistently
    4. Fonts - use Nunito (existing font) throughout
    
    Per context: "Flexible consistency" - some variation allowed,
    but new games should feel at home with existing ones.
  </action>
  <verify>
    <automated>Manual: Visual check of new games matching existing style</automated>
  </verify>
  <done>New games follow existing UI patterns</done>
</task>

</tasks>

<verification>
- All touch targets meet 48dp minimum
- Touch targets fixed by adjusting spacing (not component replacement)
- New learning games consistent with existing game patterns
- No accessibility issues in any screen
</verification>

<success_criteria>
ACCESS-01: Touch targets minimum 48dp on all screens
POLISH-03: Consistent UI patterns across all games
</success_criteria>

<output>
After completion, create `.planning/phases/02-polish-accessibility/02-03-SUMMARY.md`
</output>
