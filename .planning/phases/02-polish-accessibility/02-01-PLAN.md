---
phase: 02-polish-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/context/SettingsContext.tsx
  - src/screens/NumberPicnicScreen.tsx
  - src/screens/LetterLanternScreen.tsx
  - src/screens/StarPathScreen.tsx
  - src/screens/BubbleScreen.tsx
  - src/screens/KeepyUppyScreen.tsx
  - src/screens/CategoryMatchScreen.tsx
  - src/screens/PatternTrainScreen.tsx
  - src/screens/GlitterScreen.tsx
  - src/screens/DrawingScreen.tsx
  - src/screens/BreathingGardenScreen.tsx
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
user_setup: []

must_haves:
  truths:
    - "All games work when animationsEnabled is false"
    - "All games work when soundEnabled is false"
    - "Animated elements (loaders, spinners) are hidden when animations disabled"
    - "Sound files are not loaded/played when soundEnabled is false"
  artifacts:
    - path: "src/context/SettingsContext.tsx"
      provides: "Settings with animationsEnabled, soundEnabled"
    - path: "src/screens/*Screen.tsx"
      provides: "All game screens check settings before animating/playing sound"
  key_links:
    - from: "SettingsContext.tsx"
      to: "each game screen"
      via: "useSettings hook"
      pattern: "useSettings.*settings\\.(animationsEnabled|soundEnabled)"
---

<objective>
Implement animation and sound disable functionality across all games. When users disable animations or sound in settings, all games must work without them.
</objective>

<execution_context>
@C:/Users/james/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/02-polish-accessibility/02-CONTEXT.md

# Key Decisions from Context:
- When animationsEnabled is false: disable entirely, instant state changes
- Apply to every screen, not just games
- Navigation transitions OK (keep them)
- Skip animated elements (loaders, spinners) when disabled
- When soundEnabled is false: silent is fine, no visual replacements needed
- Remove sound files when disabled to keep app lightweight

# Already Implemented:
- SettingsContext has animationsEnabled and soundEnabled settings
- SettingsScreen has toggles for these
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit animation usage in all games</name>
  <files>
    - src/screens/NumberPicnicScreen.tsx
    - src/screens/LetterLanternScreen.tsx
    - src/screens/StarPathScreen.tsx
    - src/screens/BubbleScreen.tsx
    - src/screens/KeepyUppyScreen.tsx
    - src/screens/CategoryMatchScreen.tsx
    - src/screens/PatternTrainScreen.tsx
    - src/screens/GlitterScreen.tsx
    - src/screens/DrawingScreen.tsx
    - src/screens/BreathingGardenScreen.tsx
  </files>
  <action>
    Search each game screen for Animated.*, useAnimatedValue, Animated.timing, Animated.spring, 
    react-native-reanimated usage, and any import of animation libraries.
    Document which games use animations and for what (transitions, game mechanics, feedback).
    Note: Glitter Globe particles are essential gameplay - will handle separately.
  </action>
  <verify>
    <automated>grep -r "Animated\|reanimated\|useAnimatedValue" src/screens/*.tsx | head -30</automated>
  </verify>
  <done>List of games with animation usage documented</done>
</task>

<task type="auto">
  <name>Task 2: Add animation guards to game screens</name>
  <files>
    - src/screens/NumberPicnicScreen.tsx
    - src/screens/LetterLanternScreen.tsx
    - src/screens/StarPathScreen.tsx
    - src/screens/BubbleScreen.tsx
    - src/screens/KeepyUppyScreen.tsx
    - src/screens/CategoryMatchScreen.tsx
    - src/screens/PatternTrainScreen.tsx
    - src/screens/DrawingScreen.tsx
    - src/screens/BreathingGardenScreen.tsx
  </files>
  <action>
    In each game screen that uses animations:
    1. Import useSettings from context/SettingsContext
    2. Get settings.animationsEnabled value
    3. Wrap Animated.* calls with conditional: if (!settings.animationsEnabled) skip animation
    4. For instant transitions: use values directly without animation duration
    5. For game mechanics that require animation: add game-specific handling per context notes
    
    Handle loaders/spinners: Hide them when animations disabled (return null or show static placeholder)
  </action>
  <verify>
    <automated>grep -r "settings.animationsEnabled" src/screens/*.tsx | wc -l</automated>
  </verify>
  <done>All animated screens check animationsEnabled before animating</done>
</task>

<task type="auto">
  <name>Task 3: Audit and fix sound dependencies</name>
  <files>
    - src/screens/NumberPicnicScreen.tsx
    - src/screens/LetterLanternScreen.tsx
    - src/screens/StarPathScreen.tsx
    - src/screens/BubbleScreen.tsx
    - src/screens/KeepyUppyScreen.tsx
    - src/screens/CategoryMatchScreen.tsx
    - src/screens/PatternTrainScreen.tsx
    - src/screens/GlitterScreen.tsx
    - src/screens/DrawingScreen.tsx
    - src/screens/BreathingGardenScreen.tsx
  </files>
  <action>
    Search each game for expo-av usage, Audio.*, sound playback, play.*Sound functions.
    Document which games play sounds and when (start, correct, incorrect, complete, etc).
    
    Then modify each game:
    1. Import useSettings from context/SettingsContext  
    2. Get settings.soundEnabled value
    3. Wrap all sound playback calls with: if (settings.soundEnabled) { playSound() }
    
    Note: Don't remove sound files from the app - just don't play them when disabled.
    This keeps the app structure simple.
  </action>
  <verify>
    <automated>grep -r "soundEnabled\|expo-av\|Audio" src/screens/*.tsx | head -20</automated>
  </verify>
  <done>All games check soundEnabled before playing any audio</done>
</task>

</tasks>

<verification>
- All game screens respect animationsEnabled setting
- All game screens respect soundEnabled setting  
- App works without crashes when both are disabled
- Navigation transitions still work (they're allowed per context)
</verification>

<success_criteria>
POLISH-01: All games work with animations disabled (instant state, no animation)
POLISH-02: All games work with sound disabled (silent, no errors)
</success_criteria>

<output>
After completion, create `.planning/phases/02-polish-accessibility/02-01-SUMMARY.md`
</output>
