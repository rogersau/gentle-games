---
phase: 01-privacy-safe-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/context/SettingsContext.tsx
  - src/context/SettingsContext.test.tsx
  - src/screens/SettingsScreen.tsx
  - src/screens/SettingsScreen.test.tsx
  - src/i18n/locales/en-AU.json
  - src/i18n/locales/en-US.json
  - jest.setup.ts
autonomous: true
requirements:
  - PRIV-01
user_setup: []
must_haves:
  truths:
    - Parent can find a telemetry preference inside Settings without leaving the app.
    - A fresh install keeps telemetry disabled until a parent explicitly turns it on.
    - Changing the telemetry preference persists through the shared settings storage flow.
  artifacts:
    - path: src/types/index.ts
      provides: Shared Settings contract with telemetryEnabled
      contains: telemetryEnabled
    - path: src/context/SettingsContext.tsx
      provides: Default, sanitize, load, and persist logic for telemetryEnabled
      contains: sanitizeSettings
    - path: src/screens/SettingsScreen.tsx
      provides: Parent-facing telemetry toggle in the existing settings UI
      contains: telemetry
    - path: src/i18n/locales/en-AU.json
      provides: Calm parent-facing telemetry copy
      contains: settings.telemetry
  key_links:
    - from: src/screens/SettingsScreen.tsx
      to: src/context/SettingsContext.tsx
      via: updateSettings({ telemetryEnabled: value })
      pattern: telemetryEnabled
    - from: src/context/SettingsContext.tsx
      to: AsyncStorage
      via: gentleMatchSettings persistence
      pattern: gentleMatchSettings
---

<objective>
Create the parent-controlled telemetry preference contract so Phase 1 has a single, persisted consent source of truth.

Purpose: Land the locked decision that telemetry is an in-app parent setting, default-off, stored with the existing settings model.
Output: Updated settings types/context, telemetry toggle UI, and localized copy/tests proving persistence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-privacy-safe-bootstrap/01-CONTEXT.md
@.planning/phases/01-privacy-safe-bootstrap/01-RESEARCH.md
@src/types/index.ts
@src/context/SettingsContext.tsx
@src/screens/SettingsScreen.tsx
@src/context/SettingsContext.test.tsx
@src/screens/SettingsScreen.test.tsx

<interfaces>
From src/types/index.ts:
```typescript
export interface Settings {
  animationsEnabled: boolean;
  soundEnabled: boolean;
  soundVolume: number;
  difficulty: Difficulty;
  theme: 'animals' | 'shapes' | 'mixed';
  showCardPreview: boolean;
  keepyUppyEasyMode: boolean;
  colorMode: ColorMode;
  hiddenGames: string[];
  parentTimerMinutes: number;
  enableUnfinishedGames: boolean;
  language: import('./i18n').SupportedLanguage;
  reducedMotionEnabled: boolean;
}
```

From src/context/SettingsContext.tsx:
```typescript
interface SettingsContextType {
  settings: Settings;
  updateSettings: (newSettings: Partial<Settings>) => Promise<void>;
  isLoading: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add telemetryEnabled to the persisted settings contract</name>
  <files>src/types/index.ts, src/context/SettingsContext.tsx, src/context/SettingsContext.test.tsx</files>
  <behavior>
    - Test 1: Fresh settings default to telemetryEnabled=false.
    - Test 2: Invalid persisted telemetryEnabled values sanitize back to false using the existing boolean-setting pattern.
    - Test 3: updateSettings({ telemetryEnabled: true }) persists telemetryEnabled=true in gentleMatchSettings.
  </behavior>
  <action>Extend the shared Settings interface with telemetryEnabled, add it to defaultSettings with a false default per user decision, and sanitize/load/persist it inside SettingsContext alongside the existing boolean preferences. Reuse the current sanitizeSettings + AsyncStorage pipeline; do not create a separate storage key or parallel consent model.</action>
  <verify>
    <automated>npm test -- --runInBand src/context/SettingsContext.test.tsx</automated>
  </verify>
  <done>SettingsContext exposes telemetryEnabled, cold-start defaults keep it false, and the persisted settings payload round-trips the field safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface the parent-facing telemetry toggle with localized privacy copy</name>
  <files>src/screens/SettingsScreen.tsx, src/screens/SettingsScreen.test.tsx, src/i18n/locales/en-AU.json, src/i18n/locales/en-US.json, jest.setup.ts</files>
  <behavior>
    - Test 1: SettingsScreen renders a telemetry toggle in the existing toggle-style settings UI.
    - Test 2: Toggling the control calls updateSettings with telemetryEnabled true/false.
    - Test 3: Parent-facing copy renders calm localized label/description text instead of raw keys in app and Jest mocks.
  </behavior>
  <action>Add a telemetry section to SettingsScreen using the existing SettingToggle pattern, not a modal or separate flow, and place it with the other parent-facing controls. Add matching translation keys in both locale files plus jest.setup.ts mock coverage for the new visible strings, including the external-link fallback strings that downstream Plan 01-02 will reuse. Keep the wording calm, parent-safe, and explicit that analytics/crash reporting stay off until enabled.</action>
  <verify>
    <automated>npm test -- --runInBand src/screens/SettingsScreen.test.tsx</automated>
  </verify>
  <done>Parents can review and change telemetry consent from Settings, the control uses localized copy, and downstream plans can rely on the shared privacy strings already existing.</done>
</task>

</tasks>

<verification>
Run the settings-focused test slice, then spot-check that the plan leaves one shared consent source of truth:

- `npm test -- --runInBand src/context/SettingsContext.test.tsx src/screens/SettingsScreen.test.tsx`
- `npm run typecheck`
</verification>

<success_criteria>
- Settings has a persisted `telemetryEnabled` boolean defaulting to `false`.
- SettingsScreen shows an in-app telemetry toggle using the established settings UI.
- New telemetry copy exists in both supported locales and Jest translation mocks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-safe-bootstrap/01-01-SUMMARY.md`
</output>
