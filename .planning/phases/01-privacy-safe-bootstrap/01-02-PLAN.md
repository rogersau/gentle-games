---
phase: 01-privacy-safe-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/utils/externalLinks.ts
  - src/utils/externalLinks.test.ts
  - src/screens/HomeScreen.tsx
  - src/screens/HomeScreen.test.tsx
autonomous: true
requirements:
  - PRIV-04
user_setup: []
must_haves:
  truths:
    - Tapping the audited website link either opens the site or shows a calm in-app fallback.
    - The fallback message stays inside the app and never exposes raw technical errors.
    - The external-link path is routed through one guarded helper instead of direct Linking.openURL calls.
  artifacts:
    - path: src/utils/externalLinks.ts
      provides: Guarded audited external-link helper with simple result states
      exports:
        - openExternalUrl
    - path: src/screens/HomeScreen.tsx
      provides: Website-link handler plus calm AppModal fallback
      contains: websiteLink
    - path: src/screens/HomeScreen.test.tsx
      provides: Regression coverage for link success/failure behavior
      contains: website
  key_links:
    - from: src/screens/HomeScreen.tsx
      to: src/utils/externalLinks.ts
      via: awaited website-link handler
      pattern: openExternalUrl
    - from: src/screens/HomeScreen.tsx
      to: src/ui/components/AppModal.tsx
      via: local failure state
      pattern: AppModal
---

<objective>
Replace the audited direct website launch with a guarded helper and a gentle in-app fallback.

Purpose: Meet the privacy/support-link failure requirement without introducing a broader link-management subsystem.
Output: A narrow external-link helper, HomeScreen wiring, and tests for open/unsupported/failure paths.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-privacy-safe-bootstrap/01-CONTEXT.md
@.planning/phases/01-privacy-safe-bootstrap/01-RESEARCH.md
@src/screens/HomeScreen.tsx
@src/ui/components/AppModal.tsx

<interfaces>
From src/ui/components/AppModal.tsx:
```typescript
export const AppModal: React.FC<{
  visible: boolean;
  onClose: () => void;
  title?: string;
  children: React.ReactNode;
  contentStyle?: ViewStyle;
  showClose?: boolean;
  closeLabel?: string;
  dismissOnBackdropPress?: boolean;
}>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the guarded audited external-link helper</name>
  <files>src/utils/externalLinks.ts, src/utils/externalLinks.test.ts</files>
  <behavior>
    - Test 1: When canOpenURL returns true and openURL resolves, the helper returns 'opened'.
    - Test 2: When canOpenURL returns false, the helper returns 'unsupported' without calling openURL.
    - Test 3: When canOpenURL or openURL rejects, the helper returns 'failed' and does not throw.
  </behavior>
  <action>Create a narrow `openExternalUrl(url)` helper that awaits `Linking.canOpenURL()` and `Linking.openURL()`, returning only `'opened' | 'unsupported' | 'failed'`. Keep it generic enough for the audited path but do not invent a large link service or surface raw errors.</action>
  <verify>
    <automated>npm test -- --runInBand src/utils/externalLinks.test.ts</automated>
  </verify>
  <done>The helper safely collapses platform/link failures into simple result states that UI code can handle calmly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route HomeScreen website launches through the helper and calm fallback modal</name>
  <files>src/screens/HomeScreen.tsx, src/screens/HomeScreen.test.tsx</files>
  <behavior>
    - Test 1: Pressing the website link calls openExternalUrl for https://gentlegames.org.
    - Test 2: If the helper returns unsupported or failed, HomeScreen shows a gentle modal with the localized fallback copy added in Plan 01-01.
    - Test 3: The fallback path does not render raw exception text or call Linking.openURL directly from JSX.
  </behavior>
  <action>Replace the inline `Linking.openURL('https://gentlegames.org')` handler with an awaited helper call, then show a calm AppModal when the result is not `opened`. Keep the scope narrow to the audited website path, reuse the existing HomeScreen/AppModal UI pattern, and use the shared localized strings from Plan 01-01 instead of inline English or raw technical details.</action>
  <verify>
    <automated>npm test -- --runInBand src/utils/externalLinks.test.ts src/screens/HomeScreen.test.tsx</automated>
  </verify>
  <done>The audited external link either opens successfully or fails gently inside the app with parent-safe copy.</done>
</task>

</tasks>

<verification>
- `npm test -- --runInBand src/utils/externalLinks.test.ts src/screens/HomeScreen.test.tsx`
- `npm run typecheck`
</verification>

<success_criteria>
- HomeScreen no longer calls `Linking.openURL(...)` directly for the audited website path.
- A failed or unsupported website launch shows an in-app modal instead of a silent failure.
- Automated tests cover opened, unsupported, and failed outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-safe-bootstrap/01-02-SUMMARY.md`
</output>
