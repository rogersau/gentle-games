---
phase: 01-privacy-safe-bootstrap
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - App.tsx
  - App.test.tsx
  - src/utils/observabilityBootstrap.ts
  - src/utils/observabilityBootstrap.test.ts
  - src/utils/analytics.ts
  - src/utils/analytics.test.ts
  - src/utils/sentry.ts
  - src/utils/sentry.test.ts
  - src/components/GentleErrorBoundary.tsx
  - src/components/GentleErrorBoundary.test.tsx
autonomous: true
requirements:
  - PRIV-02
  - PRIV-03
  - STAB-02
user_setup: []
must_haves:
  truths:
    - A cold start waits for settings to load before observability services reconcile their consent state.
    - When telemetry is disabled, analytics and crash capture stay off from the first frame onward.
    - When telemetry is enabled, analytics and crash reporting send only allowlisted diagnostic fields.
    - Observability failures do not block the app from reaching the home experience.
  artifacts:
    - path: src/utils/observabilityBootstrap.ts
      provides: Central consent-aware bootstrap reconciliation after settings load
      exports:
        - reconcileObservability
    - path: App.tsx
      provides: Explicit startup flow using SettingsProvider-loaded state
      contains: reconcileObservability
    - path: src/utils/analytics.ts
      provides: Consent-aware PostHog lifecycle and allowlisted event capture
      contains: optIn
    - path: src/utils/sentry.ts
      provides: Consent-aware Sentry init/filtering and shared error capture helper
      contains: beforeSend
    - path: src/components/GentleErrorBoundary.tsx
      provides: Boundary reporting routed through shared Sentry wrapper
      contains: captureScreenError
  key_links:
    - from: App.tsx
      to: src/utils/observabilityBootstrap.ts
      via: effect after SettingsContext isLoading=false
      pattern: isLoading
   - from: src/utils/observabilityBootstrap.ts
     to: src/utils/analytics.ts
     via: reconcileObservability(telemetryEnabled)
     pattern: telemetryEnabled
    - from: src/utils/observabilityBootstrap.ts
      to: src/utils/sentry.ts
      via: consent-aware initialization and reconciliation
      pattern: initSentry
    - from: src/components/GentleErrorBoundary.tsx
      to: src/utils/sentry.ts
      via: shared capture helper
      pattern: captureScreenError
---

<objective>
Centralize consent-aware observability bootstrap so startup respects settings, remains resilient, and only emits allowlisted telemetry.

Purpose: Satisfy the privacy-safe cold-start and data-minimization requirements without scattering consent checks across screens.
Output: Explicit bootstrap wiring in App.tsx, consent-aware analytics/Sentry wrappers, and regression tests for startup ordering and filtered capture.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-privacy-safe-bootstrap/01-CONTEXT.md
@.planning/phases/01-privacy-safe-bootstrap/01-RESEARCH.md
@App.tsx
@src/utils/analytics.ts
@src/utils/sentry.ts
@src/components/GentleErrorBoundary.tsx

<interfaces>
From App.tsx:
```typescript
function getActiveRouteName(state: NavigationState | undefined): string | undefined;
export function getPostHogClient(): PostHog | null;
```

From src/utils/analytics.ts:

```typescript
export async function initAnalytics(): Promise<void>;
export function getPostHogClient(): PostHog | null;
export function trackEvent(
  eventName: string,
  properties?: Record<string, string | number | boolean | undefined>,
): void;
export function trackScreenView(screenName: string): void;
```

From src/utils/sentry.ts:

```typescript
export async function initSentry(): Promise<void>;
export function isSentryInitialized(): boolean;
export function addActionBreadcrumb(
  action: string,
  category?: string,
  data?: Record<string, unknown>,
): void;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move observability startup into an explicit post-settings bootstrap flow</name>
  <files>App.tsx, App.test.tsx, src/utils/observabilityBootstrap.ts, src/utils/observabilityBootstrap.test.ts</files>
  <behavior>
    - Test 1: App does not reconcile observability until SettingsContext has finished loading.
    - Test 2: telemetryEnabled=false leaves analytics/Sentry disabled and still renders the app shell.
    - Test 3: bootstrap failures log a warning and continue to the app instead of blocking startup.
  </behavior>
  <action>Remove the module-load `void initSentry()` and `void initAnalytics()` side effects from App.tsx. Add a centralized bootstrap helper/module that runs after settings load, reads `settings.telemetryEnabled`, and reconciles both Sentry and analytics in one place. Keep startup resilient, keep `NavigationContainer.onStateChange` manual screen tracking, and do not scatter `if (telemetryEnabled)` checks across screens or utilities.</action>
  <verify>
    <automated>npm test -- --runInBand App.test.tsx src/utils/observabilityBootstrap.test.ts</automated>
  </verify>
  <done>Cold starts use a single explicit bootstrap path after settings are available, Sentry and analytics are both reconciled from that path, and app rendering remains non-blocking even when observability setup fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce consent-aware allowlists in analytics, Sentry, and error-boundary capture</name>
  <files>src/utils/analytics.ts, src/utils/analytics.test.ts, src/utils/sentry.ts, src/utils/sentry.test.ts, src/components/GentleErrorBoundary.tsx, src/components/GentleErrorBoundary.test.tsx</files>
  <behavior>
    - Test 1: With telemetry disabled, analytics capture/screen tracking and Sentry capture become safe no-ops.
    - Test 2: With telemetry enabled, analytics events only forward allowlisted flat diagnostic keys and preserve manual screen tracking.
    - Test 3: Sentry beforeSend/beforeBreadcrumb and the error boundary drop free-form fields such as raw componentStack or ad hoc error text while keeping the anonymous install ID model.
  </behavior>
  <action>Refactor `analytics.ts` and `sentry.ts` into shared consent-aware wrappers. Use PostHog `optIn()` / `optOut()` for immediate in-session consent changes, preserve the existing anonymous install ID approach, keep `captureScreens: false`, and explicitly avoid richer person/group/session features. In Sentry, replace regex-only scrubbing with allowlisted filtering via wrapper helpers plus `beforeSend` / `beforeBreadcrumb`, add a shared `captureScreenError`-style helper, and make GentleErrorBoundary call that helper instead of the SDK directly.</action>
  <verify>
    <automated>npm test -- --runInBand src/utils/analytics.test.ts src/utils/sentry.test.ts src/components/GentleErrorBoundary.test.tsx</automated>
  </verify>
  <done>Telemetry only initializes and captures when consent allows it, payloads are allowlisted, and all boundary-driven crash reporting flows through the shared wrapper.</done>
</task>

</tasks>

<verification>
- `npm test -- --runInBand App.test.tsx src/utils/observabilityBootstrap.test.ts src/utils/analytics.test.ts src/utils/sentry.test.ts src/components/GentleErrorBoundary.test.tsx`
- `npm run typecheck`
</verification>

<success_criteria>

- App.tsx no longer initializes analytics or Sentry at module scope.
- Settings load happens before observability reconciliation.
- Telemetry-off sessions do not initialize or capture analytics/Sentry events.
- Telemetry-on sessions use allowlisted analytics and Sentry payload shaping only.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-safe-bootstrap/01-03-SUMMARY.md`
</output>
