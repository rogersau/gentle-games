---
phase: 04-release-confidence-and-regression-guardrails
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - App.tsx
  - App.test.tsx
  - src/utils/observabilityBootstrap.ts
  - src/utils/observabilityBootstrap.test.ts
  - src/utils/analytics.ts
  - src/utils/analytics.test.ts
  - src/utils/analytics-fallback.test.ts
  - src/utils/sentry.ts
  - src/utils/sentry.test.ts
autonomous: true
requirements:
  - RELG-02
must_haves:
  truths:
    - "Startup regression coverage proves the app waits for settings before bootstrapping observability and still renders safely when one or both observability services fail."
    - "Automated tests prove telemetry-enabled startup still reconciles both analytics and Sentry through the shared bootstrap seam."
    - "Telemetry wrapper regressions catch install-ID persistence, repeated-enable idempotence, and disable-after-enable opt-out behavior before release."
  artifacts:
    - path: "App.test.tsx"
      provides: "AppContent startup-loading, enabled/disabled, and failure-path regression coverage"
      contains: "reconcileObservability"
    - path: "src/utils/observabilityBootstrap.test.ts"
      provides: "Partial-failure and aggregated-error coverage for bootstrap reconciliation"
      contains: "Promise.allSettled"
    - path: "src/utils/sentry.test.ts"
      provides: "Install-ID and consent lifecycle coverage for Sentry bootstrap"
      contains: "reconcileSentryConsent"
    - path: "src/utils/analytics.test.ts"
      provides: "Consent lifecycle coverage for analytics opt-in/opt-out and install ID reuse"
      contains: "reconcileAnalyticsConsent"
  key_links:
    - from: "App.tsx"
      to: "src/utils/observabilityBootstrap.ts"
      via: "AppContent bootstrap effect after settings load"
      pattern: "reconcileObservability\\(settings\\.telemetryEnabled\\)"
    - from: "src/utils/observabilityBootstrap.ts"
      to: "src/utils/sentry.ts"
      via: "aggregated consent reconciliation"
      pattern: "reconcileSentryConsent"
    - from: "src/utils/observabilityBootstrap.ts"
      to: "src/utils/analytics.ts"
      via: "aggregated consent reconciliation"
      pattern: "reconcileAnalyticsConsent"
---

<objective>
Extend startup telemetry and observability bootstrap regression coverage around the exact seams Phase 1 introduced.

Purpose: Catch consent, loading-order, partial-failure, install-ID, and opt-out regressions before release without weakening the privacy-safe bootstrap behavior already shipped.
Output: Stronger App/bootstrap, Sentry, and analytics regression tests with only minimal production fixes where tests expose a real contract gap.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-confidence-and-regression-guardrails/04-CONTEXT.md
@.planning/phases/04-release-confidence-and-regression-guardrails/04-RESEARCH.md
@.planning/phases/01-privacy-safe-bootstrap/01-03-SUMMARY.md
@App.tsx
@App.test.tsx
@src/utils/observabilityBootstrap.ts
@src/utils/observabilityBootstrap.test.ts
@src/utils/analytics.ts
@src/utils/analytics.test.ts
@src/utils/analytics-fallback.test.ts
@src/utils/sentry.ts
@src/utils/sentry.test.ts

<interfaces>
```typescript
// App.tsx
export const AppContent: React.FC = () => {
  const { settings, isLoading } = useSettings();

  useEffect(() => {
    if (isLoading) return;
    void reconcileObservability(settings.telemetryEnabled).catch((error: unknown) => {
      console.warn(
        'Observability bootstrap failed. Continuing without analytics or crash reporting.',
        error,
      );
    });
  }, [isLoading, settings.telemetryEnabled]);
}
```

```typescript
// src/utils/observabilityBootstrap.ts
export async function reconcileObservability(telemetryEnabled: boolean): Promise<void>;
```

```typescript
// src/utils/analytics.ts / src/utils/sentry.ts
export async function reconcileAnalyticsConsent(enabled: boolean): Promise<void>;
export async function reconcileSentryConsent(enabled: boolean): Promise<void>;
export function setAnalyticsUser(installId: string): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand App bootstrap and observability aggregation regressions</name>
  <files>App.test.tsx, App.tsx, src/utils/observabilityBootstrap.test.ts, src/utils/observabilityBootstrap.ts</files>
  <behavior>
    - Test 1: `AppContent` does not bootstrap observability while settings are loading, then reconciles exactly once when loading completes.
    - Test 2: `AppContent` covers both disabled and enabled telemetry startup permutations and continues rendering after bootstrap rejection with the existing calm warning.
    - Test 3: `reconcileObservability` throws predictable aggregated messages when only Sentry fails, only analytics fails, or both fail.
  </behavior>
  <action>Extend `App.test.tsx` and `src/utils/observabilityBootstrap.test.ts` around the Phase 1 bootstrap seam rather than adding a new startup abstraction. Cover rerender transitions from loading to ready and disabled to enabled telemetry so regressions in effect dependencies become visible. If the tests expose a real mismatch in `App.tsx` or `src/utils/observabilityBootstrap.ts`, fix it minimally while preserving the Phase 1 decisions: bootstrap only after settings load, no telemetry before consent, and non-blocking startup failures. Do not move observability initialization back to module load or add new startup state machines.</action>
  <verify>
    <automated>npm test -- --runInBand App.test.tsx src/utils/observabilityBootstrap.test.ts</automated>
  </verify>
  <done>The app-level startup seam is regression-tested for loading, enabled/disabled telemetry, and partial-failure aggregation without changing the shipped privacy-safe bootstrap flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover Sentry and analytics consent lifecycle edge cases</name>
  <files>src/utils/sentry.test.ts, src/utils/sentry.ts, src/utils/analytics.test.ts, src/utils/analytics-fallback.test.ts, src/utils/analytics.ts</files>
  <behavior>
    - Test 1: `reconcileSentryConsent(true)` persists a generated install ID when storage is empty, survives storage failure without crashing, and does not reinitialize Sentry on repeated enable calls.
    - Test 2: `reconcileAnalyticsConsent(true)` identifies a pending install ID after client creation, and `reconcileAnalyticsConsent(false)` opts out after a prior enable.
    - Test 3: Existing allowlist and safe-no-op guarantees remain intact alongside the new lifecycle coverage.
  </behavior>
  <action>Extend the existing wrapper test files instead of introducing new observability test modules. Use the repo’s current `jest.resetModules()` / `jest.doMock(...)` pattern so each suite can control AsyncStorage, Sentry, PostHog, and `__DEV__` state deterministically. If the new regressions expose a true lifecycle bug in `src/utils/sentry.ts` or `src/utils/analytics.ts`, patch only that bug while preserving current public helper names and allowlist behavior. Do not broaden scope into new telemetry event types, extra analytics fields, or different consent storage.</action>
  <verify>
    <automated>npm test -- --runInBand src/utils/sentry.test.ts src/utils/analytics.test.ts src/utils/analytics-fallback.test.ts</automated>
  </verify>
  <done>Sentry and analytics lifecycle tests now cover install-ID, opt-in/out, and repeated-enable behavior without weakening the existing privacy and allowlist guarantees.</done>
</task>

</tasks>

<verification>
Run the startup-focused suite end to end:

`npm test -- --runInBand App.test.tsx src/utils/observabilityBootstrap.test.ts src/utils/sentry.test.ts src/utils/analytics.test.ts src/utils/analytics-fallback.test.ts`
</verification>

<success_criteria>
- Startup regressions cover loading, consent-enabled, consent-disabled, and partial-failure permutations through the actual bootstrap seam.
- Sentry and PostHog wrapper tests cover install-ID, repeated enable, and disable-after-enable lifecycle behavior.
- No task reintroduces telemetry before consent or weakens the Phase 1 allowlist decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-confidence-and-regression-guardrails/04-03-SUMMARY.md`
</output>
