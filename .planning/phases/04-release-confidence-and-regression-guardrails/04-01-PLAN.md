---
phase: 04-release-confidence-and-regression-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/numberpicnic/PicnicBlanket.test.tsx
  - src/components/CategoryMatchBoard.tsx
  - src/components/CategoryMatchBoard.test.tsx
  - src/screens/CategoryMatchScreen.test.tsx
  - src/screens/BubbleScreen.tsx
  - src/screens/BubbleScreen.test.tsx
  - src/screens/GameScreen.tsx
  - src/screens/GameScreen.test.tsx
autonomous: true
requirements:
  - RELG-04
must_haves:
  truths:
    - "The shared Jest baseline no longer fails on a stale Number Picnic threshold assertion and instead reflects the shipped overlap-based drop contract."
    - "Bubble Pop route coverage proves the screen still exposes its popped counter through the accessibility surface after real pop interactions."
    - "Category Match regression coverage proves preview-to-board flow, correct and incorrect sorting feedback, and streak messaging still work on the audited surface."
    - "Memory Snap route-wrapper coverage proves the screen header, back action, and stats accessibility label remain wired around the real board contract."
  artifacts:
    - path: "src/components/numberpicnic/PicnicBlanket.test.tsx"
      provides: "Overlap-based Number Picnic drag/drop regression coverage"
      contains: "dropZoneLayout"
    - path: "src/components/CategoryMatchBoard.test.tsx"
      provides: "Real board interaction coverage for hover, correct/incorrect drop, and timer cleanup"
      contains: "category-draggable-token"
    - path: "src/screens/CategoryMatchScreen.test.tsx"
      provides: "Route-level preview, counter, and streak regressions for Category Match"
      contains: "Start Sorting"
    - path: "src/screens/BubbleScreen.test.tsx"
      provides: "Bubble Pop route-wrapper and accessibility-counter regression coverage"
      contains: "Popped:"
    - path: "src/screens/GameScreen.test.tsx"
      provides: "Memory Snap route-wrapper regression coverage"
      contains: "Time"
  key_links:
    - from: "src/components/numberpicnic/PicnicBlanket.test.tsx"
      to: "src/components/numberpicnic/PicnicBlanket.tsx"
      via: "overlap-based drag release assertions"
      pattern: "dropZoneLayout|measureInWindow|onItemDrop"
    - from: "src/screens/CategoryMatchScreen.test.tsx"
      to: "src/components/CategoryMatchBoard.test.tsx"
      via: "screen-to-board preview and match callbacks"
      pattern: "Start Sorting|onCorrectMatch|onIncorrectMatch"
    - from: "src/screens/GameScreen.test.tsx"
      to: "src/screens/GameScreen.tsx"
      via: "renderStats and onBackPress contract checks"
      pattern: "accessibilityLabel=\\{`Time \\$\\{time\\}, \\$\\{moves\\} moves`\\}"
---

<objective>
Restore a trustworthy gameplay-regression baseline and add focused audited route/interaction guardrails for Bubble Pop, Category Match, and Memory Snap.

Purpose: Close the stale Number Picnic blocker first, then lock in the milestone’s highest-risk routed gameplay surfaces without broadening Phase 4 into app-wide test expansion.
Output: Repaired Number Picnic regression expectations plus targeted Jest coverage for Bubble Pop, Category Match, and Memory Snap route-wrapper and interaction-sensitive behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-confidence-and-regression-guardrails/04-CONTEXT.md
@.planning/phases/04-release-confidence-and-regression-guardrails/04-RESEARCH.md
@.planning/phases/02-correct-and-safe-gameplay-interactions/02-02-SUMMARY.md
@.planning/phases/02-correct-and-safe-gameplay-interactions/02-03-SUMMARY.md
@.planning/phases/03-stable-navigation-and-responsive-surfaces/03-01-SUMMARY.md
@src/components/numberpicnic/PicnicBlanket.tsx
@src/components/numberpicnic/PicnicBlanket.test.tsx
@src/components/CategoryMatchBoard.tsx
@src/screens/CategoryMatchScreen.tsx
@src/screens/CategoryMatchScreen.test.tsx
@src/screens/BubbleScreen.tsx
@src/screens/BubbleScreen.test.tsx
@src/screens/GameScreen.tsx
@src/components/GameBoard.test.tsx

<interfaces>
```typescript
// src/components/numberpicnic/PicnicBlanket.tsx
export const doesNumberPicnicRectOverlap = (
  draggedRect: WindowRect | null,
  dropZoneLayout: WindowRect | null | undefined
): boolean;

interface PicnicBlanketProps {
  onItemDrop: (index: number) => void;
  onDragOverBasket?: (isOver: boolean) => void;
  dropZoneLayout?: WindowRect | null;
  onDropStart?: () => void;
  onDropEnd?: () => void;
}
```

```typescript
// src/components/CategoryMatchBoard.tsx
interface CategoryMatchBoardProps {
  width: number;
  height: number;
  onCorrectMatch?: (item: CategoryMatchItem, category: CategoryMatchCategory) => void;
  onIncorrectMatch?: () => void;
}
```

```typescript
// src/screens/GameScreen.tsx
<GameBoard
  onGameComplete={handleGameComplete}
  onBackPress={handleBackPress}
  bottomInset={insets.bottom}
  renderStats={({ time, moves }) => (
    <Text accessibilityLabel={`Time ${time}, ${moves} moves`}>
      Time: {time} · Moves: {moves}
    </Text>
  )}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stale PicnicBlanket threshold assertions with overlap-based regression coverage</name>
  <files>src/components/numberpicnic/PicnicBlanket.test.tsx</files>
  <action>Rewrite the stale Number Picnic component tests so they match Phase 2’s locked visible-overlap contract instead of the removed upward-threshold heuristic. Keep the existing available-item-count and emoji-refresh checks, but replace the positive/negative drag-release cases with tests that provide a real `dropZoneLayout` and seed draggable-item measurement through the component’s current measurement seam (`measureInWindow` or cached layout ownership). Assert one overlapping release calls `onItemDrop(0)` and one non-overlapping release does not. Do not change `PicnicBlanket.tsx` back toward distance/threshold logic; this task exists to realign the test baseline with shipped behavior.</action>
  <verify>
    <automated>npm test -- --runInBand src/components/numberpicnic/PicnicBlanket.test.tsx src/screens/NumberPicnicScreen.test.tsx</automated>
  </verify>
  <done>`PicnicBlanket.test.tsx` no longer mentions threshold-only drop rules, and the component regression suite passes against overlap-driven drop behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real Category Match board regressions and extend the screen flow checks</name>
  <files>src/components/CategoryMatchBoard.test.tsx, src/components/CategoryMatchBoard.tsx, src/screens/CategoryMatchScreen.test.tsx</files>
  <behavior>
    - Test 1: Dragging the live token over a zone highlights that zone, a correct drop calls `onCorrectMatch`, and an incorrect drop calls `onIncorrectMatch` and snaps feedback back to neutral.
    - Test 2: Feedback timers are cleared on unmount so delayed feedback does not mutate state after the screen leaves.
    - Test 3: `CategoryMatchScreen` hides the preview after Start Sorting, increments Correct count from board callbacks, and shows the streak encouragement after the third consecutive correct match.
  </behavior>
  <action>Create `src/components/CategoryMatchBoard.test.tsx` as the missing real interaction seam for RELG-04, using the repository’s existing PanResponder and fake-timer Jest patterns rather than a new test harness. Extend `src/screens/CategoryMatchScreen.test.tsx` so the screen-level preview/card wrapper remains covered while the new board test owns the drag/drop contract. If stable test access requires tiny source adjustments in `CategoryMatchBoard.tsx`, keep them minimal and accessibility-safe (for example a `testID` or preserving an existing accessible label) without changing the visual flow, round generation, or game rules.</action>
  <verify>
    <automated>npm test -- --runInBand src/components/CategoryMatchBoard.test.tsx src/screens/CategoryMatchScreen.test.tsx</automated>
  </verify>
  <done>Category Match has one real board interaction regression file plus stronger screen-flow coverage proving preview exit, correct-count updates, incorrect-drop handling, and streak feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cover Bubble Pop and Memory Snap route-wrapper accessibility contracts</name>
  <files>src/screens/BubbleScreen.test.tsx, src/screens/BubbleScreen.tsx, src/screens/GameScreen.test.tsx, src/screens/GameScreen.tsx</files>
  <behavior>
    - Test 1: Bubble Pop still renders its screen wrapper, increments the popped counter on `onBubblePop`, and exposes the translated counter through the screen-level accessibility label.
    - Test 2: `GameScreen` still renders the Memory Snap header, wires back navigation through `onBackPress`, and exposes the `renderStats` accessibility label in the route wrapper.
    - Test 3: Any source tweak added for test stability preserves the existing user-visible route flow and does not replace the real `GameBoard` contract with duplicated logic.
  </behavior>
  <action>Extend `BubbleScreen.test.tsx` beyond visible text so the test asserts the screen’s accessibility-facing popped-count contract, not just the mocked child callback. Add `src/screens/GameScreen.test.tsx` to verify the route wrapper around `GameBoard` still passes `onBackPress`, renders the Memory Snap title/header chrome, and publishes the stats text through the existing `renderStats` accessibility label. Modify `BubbleScreen.tsx` or `GameScreen.tsx` only if a small, stable, accessibility-aligned seam is needed for deterministic assertions; do not redesign the screens or duplicate logic already covered by `BubbleField.test.tsx` and `GameBoard.test.tsx`.</action>
  <verify>
    <automated>npm test -- --runInBand src/screens/BubbleScreen.test.tsx src/screens/GameScreen.test.tsx src/components/BubbleField.test.tsx src/components/GameBoard.test.tsx</automated>
  </verify>
  <done>Bubble Pop and Memory Snap both have focused route-wrapper regressions that prove their accessible counters/header wiring still match the audited screen contracts.</done>
</task>

</tasks>

<verification>
Run the repaired Number Picnic component regression first, then run the targeted RELG-04 suite:

`npm test -- --runInBand src/components/numberpicnic/PicnicBlanket.test.tsx src/components/CategoryMatchBoard.test.tsx src/screens/CategoryMatchScreen.test.tsx src/screens/BubbleScreen.test.tsx src/screens/GameScreen.test.tsx src/components/BubbleField.test.tsx src/components/GameBoard.test.tsx`
</verification>

<success_criteria>
- `PicnicBlanket.test.tsx` reflects overlap-based drop semantics and no longer blocks shared Jest with stale threshold expectations.
- Bubble Pop, Category Match, and Memory Snap each have focused regression coverage on the real route or interaction seam identified in research.
- The new coverage stays inside existing Jest/react-native-testing-library patterns and does not add a new framework or broaden into unrelated screens.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-confidence-and-regression-guardrails/04-01-SUMMARY.md`
</output>
