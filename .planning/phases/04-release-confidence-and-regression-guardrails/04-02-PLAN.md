---
phase: 04-release-confidence-and-regression-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scripts/upload-sourcemaps.js
  - scripts/upload-sourcemaps.test.js
autonomous: true
requirements:
  - RELG-01
  - RELG-03
must_haves:
  truths:
    - 'Release fallback automation looks for web source maps in the real web export root and keeps native fallback paths pointed at their existing platform directories.'
    - 'Automated regression coverage fails if web path resolution drifts back to `dist/web` or accidentally absorbs native artifacts from `dist/android` or `dist/ios`.'
    - 'The fallback upload script still skips missing or sourcemap-free platforms gracefully instead of failing the whole release path immediately.'
  artifacts:
    - path: 'scripts/upload-sourcemaps.js'
      provides: 'Explicit platform-output mapping and fallback upload behavior'
      contains: 'uploadSourceMaps'
    - path: 'scripts/upload-sourcemaps.test.js'
      provides: 'Deterministic Jest regression coverage for web/android/ios fallback path assumptions'
      contains: 'web'
  key_links:
    - from: 'scripts/upload-sourcemaps.test.js'
      to: 'scripts/upload-sourcemaps.js'
      via: 'mocked fs/path/execSync assertions'
      pattern: 'dist|android|ios|execSync'
    - from: 'package.json'
      to: 'scripts/upload-sourcemaps.js'
      via: 'build:web and validate:android/ios output roots'
      pattern: 'build:web|validate:android|validate:ios'
---

<objective>
Repair the Sentry fallback source-map path assumptions and lock them down with automated regression coverage.

Purpose: Keep release debugging trustworthy across web, Android, and iOS without introducing a new release subsystem or relying on manual source-map upload checks.
Output: Explicit platform-output handling in the existing fallback script plus a new Jest script suite that guards web/native path behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-confidence-and-regression-guardrails/04-CONTEXT.md
@.planning/phases/04-release-confidence-and-regression-guardrails/04-RESEARCH.md
@package.json
@.github/workflows/ci.yml
@scripts/upload-sourcemaps.js
@scripts/prepare-pwa.test.js

<interfaces>
```javascript
// package.json
"build:web": "expo export --platform web --output-dir dist --clear",
"validate:android": "expo export --platform android --output-dir dist/android",
"validate:ios": "expo export --platform ios --output-dir dist/ios",
"upload:sourcemaps": "node scripts/upload-sourcemaps.js"
```

```javascript
// scripts/upload-sourcemaps.js
function findSourceMaps(distDir) {
  /* recursive scan */
}

function uploadSourceMaps() {
  const platforms = ['web', 'ios', 'android'];
  for (const platform of platforms) {
    const distDir = path.join(__dirname, '..', 'dist', platform);
    // ...
  }
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the fallback upload script importable and add platform-path regression tests</name>
  <files>scripts/upload-sourcemaps.test.js, scripts/upload-sourcemaps.js</files>
  <behavior>
    - Test 1: Web resolves to `dist/`, while Android and iOS resolve to `dist/android` and `dist/ios`.
    - Test 2: Web scanning does not count native artifacts living under `dist/android` or `dist/ios` after a combined build.
    - Test 3: Missing directories or zero `.map` files cause a skip log instead of a thrown failure.
  </behavior>
  <action>Create `scripts/upload-sourcemaps.test.js` using the same mocked Node-script pattern already established in `scripts/prepare-pwa.test.js`: mock `fs`, `path`, `child_process.execSync`, console methods, and any exit behavior so the script can be imported safely under Jest. Refactor `scripts/upload-sourcemaps.js` behind a `require.main === module` guard and export only the minimum helpers needed for deterministic testing. Keep the script in CommonJS and in the existing fallback location per user decision; do not introduce a new CLI wrapper, release library, or workflow step.</action>
  <verify>
    <automated>npm test -- --runInBand scripts/upload-sourcemaps.test.js</automated>
  </verify>
  <done>The fallback upload script can be imported under Jest without side effects, and there is explicit failing coverage for wrong web/native path assumptions before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit web/android/ios fallback roots and preserve current release commands</name>
  <files>scripts/upload-sourcemaps.js, scripts/upload-sourcemaps.test.js</files>
  <behavior>
    - Test 1: `uploadSourceMaps` uploads from the real web root instead of `dist/web`.
    - Test 2: CLI invocations preserve platform-specific `--url-prefix` usage while only uploading when `.map` files exist for that platform root.
    - Test 3: The script still logs success/skip behavior cleanly and leaves `npm run build:web`, `npm run validate:android`, and `npm run validate:ios` unchanged.
  </behavior>
  <action>Replace the inferred `dist/<platform>` assumption with an explicit platform-output map that matches the current package scripts: web => `dist/`, Android => `dist/android`, iOS => `dist/ios`. For web, constrain traversal so native subdirectories are not mistaken for web sourcemaps after `build:all`. Keep the existing fallback upload semantics, env var contract, and `sentry:release` entrypoint intact. Use the new Jest suite to prove the path logic, then run the real build/export commands as verification only; do not broaden this into a Sentry release redesign or speculative `url-prefix` overhaul beyond the known root-path bug.</action>
  <verify>
    <automated>npm test -- --runInBand scripts/upload-sourcemaps.test.js</automated>
  </verify>
  <done>The fallback script resolves the actual export roots for web, Android, and iOS, and automated tests prevent the audited web-path regression from returning.</done>
</task>

</tasks>

<verification>
Run `npm test -- --runInBand scripts/upload-sourcemaps.test.js` first, then verify the existing export commands still succeed:

`npm run build:web && npm run validate:android && npm run validate:ios`
</verification>

<success_criteria>

- `scripts/upload-sourcemaps.js` keeps the existing fallback workflow but no longer looks for web artifacts in `dist/web`.
- `scripts/upload-sourcemaps.test.js` guards web, Android, and iOS fallback-path assumptions and catches web/native subtree mix-ups.
- Existing package build/export entrypoints remain unchanged and usable by CI.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-release-confidence-and-regression-guardrails/04-02-SUMMARY.md`
</output>
