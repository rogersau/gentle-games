---
phase: 02-correct-and-safe-gameplay-interactions
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/utils/useTrackedTimeouts.ts
  - src/utils/useTrackedTimeouts.test.ts
  - src/utils/numberPicnicLogic.ts
  - src/utils/numberPicnicLogic.test.ts
  - src/components/GameBoard.tsx
  - src/components/GameBoard.test.tsx
autonomous: true
requirements:
  - STAB-03
  - PERF-03
must_haves:
  truths:
    - "Leaving Number Picnic does not allow delayed processing-reset callbacks to mutate state after unmount."
    - "Rapid replay in Memory Snap does not leave stale preview or match-resolution timers running."
    - "Audited timer-driven gameplay callbacks are tracked and cancellable instead of raw fire-and-forget timeouts."
  artifacts:
    - path: "src/utils/useTrackedTimeouts.ts"
      provides: "Shared tracked timeout registry with clear-all cleanup"
      exports: ["useTrackedTimeouts"]
    - path: "src/utils/numberPicnicLogic.ts"
      provides: "Replay-safe Number Picnic timer cleanup"
      contains: "clearAllTimeouts"
    - path: "src/components/GameBoard.tsx"
      provides: "Tracked preview and match-resolution timers"
      contains: "clearAllTimeouts"
    - path: "src/components/GameBoard.test.tsx"
      provides: "Fake-timer regressions for unmount and replay cleanup"
      contains: "replay"
  key_links:
    - from: "src/utils/useTrackedTimeouts.ts"
      to: "src/utils/numberPicnicLogic.ts"
      via: "queued processing-reset timeout"
      pattern: "queueTimeout"
    - from: "src/utils/useTrackedTimeouts.ts"
      to: "src/components/GameBoard.tsx"
      via: "queued preview and match-resolution timers"
      pattern: "queueTimeout"
    - from: "src/components/GameBoard.tsx"
      to: "new round and unmount cleanup"
      via: "clearAllTimeouts before scheduling replacements"
      pattern: "clearAllTimeouts"
---

<objective>
Make the audited timer-driven gameplay flows replay-safe and unmount-safe by replacing raw delayed callbacks with tracked cancellable timers.

Purpose: Stop stale timeouts from mutating gameplay after navigation away, replay, or rapid repeated interactions.
Output: Shared tracked-timeout helper plus Number Picnic and Memory Snap timer cleanup regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-correct-and-safe-gameplay-interactions/02-CONTEXT.md
@.planning/phases/02-correct-and-safe-gameplay-interactions/02-RESEARCH.md
@src/utils/numberPicnicLogic.ts
@src/utils/numberPicnicLogic.test.ts
@src/components/GameBoard.tsx
@src/components/GameBoard.test.tsx
@src/screens/usePatternTrainGame.ts
@src/screens/usePatternTrainGame.test.ts

<interfaces>
Reference timeout pattern from `src/screens/usePatternTrainGame.ts`:
```typescript
const timeoutIdsRef = useRef<ReturnType<typeof setTimeout>[]>([]);

const queueTimeout = useCallback((callback: () => void, delay: number) => {
  const timeoutId = setTimeout(() => {
    timeoutIdsRef.current = timeoutIdsRef.current.filter((id) => id !== timeoutId);
    callback();
  }, delay);
  timeoutIdsRef.current.push(timeoutId);
}, []);

const clearAllTimeouts = useCallback(() => {
  timeoutIdsRef.current.forEach((timeoutId) => clearTimeout(timeoutId));
  timeoutIdsRef.current = [];
}, []);
```

Current Number Picnic timer seam:
```typescript
setTimeout(() => {
  setIsProcessing(false);
}, 300);
```

Current Memory Snap timer seams:
```typescript
previewTimerRef.current = setTimeout(..., 2000);
setTimeout(() => { ... }, 500);
setTimeout(() => { ... }, 1000);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract tracked timeout cleanup and apply it to Number Picnic processing timers</name>
  <files>src/utils/useTrackedTimeouts.ts, src/utils/useTrackedTimeouts.test.ts, src/utils/numberPicnicLogic.ts, src/utils/numberPicnicLogic.test.ts</files>
  <behavior>
    - Test 1: `queueTimeout` runs callbacks once and removes completed timer IDs from the registry.
    - Test 2: `clearAllTimeouts` cancels pending callbacks and unmount cleanup leaves no timers behind.
    - Test 3: Number Picnic replay or unmount cannot let a pending processing-reset timeout flip `isProcessing` later.
  </behavior>
  <action>Create a tiny reusable `useTrackedTimeouts` helper modeled on `usePatternTrainGame`, with `queueTimeout`, `clearAllTimeouts`, and unmount cleanup. Add focused fake-timer tests for the helper. Refactor `useNumberPicnicGame` to use the helper for processing-reset delays and clear pending timers when the hook unmounts and before a new round begins. Keep the gameplay logic local and simple; do not introduce a state machine or widen this into a repo-wide timer rewrite.</action>
  <verify>
    <automated>npm test -- --runInBand src/utils/useTrackedTimeouts.test.ts src/utils/numberPicnicLogic.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Number Picnic uses tracked cancellable timers, helper coverage passes, and pending processing timeouts cannot fire after unmount or replay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Memory Snap match and preview timers to clear on replay and unmount</name>
  <files>src/components/GameBoard.tsx, src/components/GameBoard.test.tsx, src/utils/useTrackedTimeouts.ts</files>
  <behavior>
    - Test 1: Starting a new game during preview clears the old preview timeout before a new one is scheduled.
    - Test 2: Match and mismatch resolution timers do not fire after the board unmounts.
    - Test 3: Rapid play-again interactions do not apply stale flips or completion state to the next round.
  </behavior>
  <action>Replace the raw timeout usage in `GameBoard.tsx` with the tracked helper while preserving the current preview phase, move counter, and completion flow. Clear all pending gameplay timers at the start of `startNewGame` and in unmount cleanup before scheduling replacements. Expand `GameBoard.test.tsx` with fake-timer regressions for replay and unmount cleanup. Do not turn this into a broader refactor of tile generation, sound playback, or board rendering.</action>
  <verify>
    <automated>npm test -- --runInBand src/components/GameBoard.test.tsx src/utils/useTrackedTimeouts.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>GameBoard no longer leaves stale delayed callbacks behind across unmount or replay, and the new timer-cleanup regressions pass.</done>
</task>

</tasks>

<verification>
Run `npm test -- --runInBand src/utils/useTrackedTimeouts.test.ts src/utils/numberPicnicLogic.test.ts src/components/GameBoard.test.tsx` and `npm run typecheck`.
</verification>

<success_criteria>
- Number Picnic delayed processing resets cannot fire after the hook unmounts or after a fresh round starts.
- Memory Snap preview and match-resolution timers are cancelled before replay and during unmount cleanup.
- The audited timer-driven regressions pass with fake timers and strict TypeScript still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-correct-and-safe-gameplay-interactions/02-03-SUMMARY.md`
</output>
